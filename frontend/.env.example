# Backend host for the FastAPI service (api_service.py). No scheme means
# ws:// is assumed; use an explicit http://... or https://... to control
# ws vs wss (see components/WebSocketAlertPanel.tsx's buildAlertsWsUrl()).
NEXT_PUBLIC_API_HOST=localhost:8000
